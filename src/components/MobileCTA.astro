---
interface Props {
  phone: string;
}

const { phone } = Astro.props;
const phoneDigits = phone?.replace(/[^\d+]/g, '') || '';
---

{phone && (
  <div class="mobile-cta" aria-label="Call us">
    <a href={`tel:${phoneDigits}`}>
      Call {phone}
    </a>
  </div>
)}
